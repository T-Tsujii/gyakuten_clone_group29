
<h1>動画一覧</h1>
<table>
  <thead>
  <tr>
    <th scope="col">題名</th>
    <th scope="col">リンク</th>
  </tr>
  </thead>
  <tbody>
  <% @movies.each.with_index(1) do |movie, i| %>
    <tr>
      <td><%= movie.title %></td>
      <td><%= movie.url %></td>
    </tr>
  <% end %>
  </tbody>
</table>